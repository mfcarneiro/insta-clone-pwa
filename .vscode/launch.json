{
  "version": "0.2.0",
  "configurations": [
    
    {
      "type": "chrome",
      "request": "attach",
      "name": "Launch Chrome against localhost",
      "urlFilter": "http://localhost:8080/*",
      "port": 9222,
      "webRoot": "${workspaceFolder}"
      
    }
  ]
}